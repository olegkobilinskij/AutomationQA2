const circleRadius = 26;
const areaOfCircle = Math.PI * Math.pow(circleRadius, 2);

console.log(areaOfCircle);

const length = 12;
const width = 27;
const areaOfRectangle = length * width;
console.log(areaOfRectangle);

const cylinderRadius = 28;
const cylinderHeight = 7;
const volumeOfCylinder = (Math.PI * Math.pow(cylinderRadius, 2) * cylinderHeight);
console.log(volumeOfCylinder.toFixed(2));

